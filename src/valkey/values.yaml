architecture: replication

replicaCount: 3

persistence:
  enabled: true
  storageClass: local-path

auth:
  enabled: true
  password: "password" # Change me!

resources:
  limits:
    cpu: "1"
    memory: "2Gi"
  requests:
    cpu: "0.1"
    memory: "512Mi"
